# Products Endpoints Guide - دو نقش متفاوت

## 🛍️ دو Endpoint مختلف برای دو مقصود مختلف

### 1. **GET `/products` - محصولات برای مشتریان** 
   - ✅ **فقط محصولات با Offer**
   - ✅ فیلترهای پیشرفته (قیمت، تخفیف، موجودی، etc)
   - ✅ برای صفحات عمومی و سایت
   - ✅ مناسب برای SSR/SEO

```bash
GET /products?categoryId=5&sortBy=price_low&inStockOnly=true
```

**پاسخ**: تنها محصولات موجود با قیمت کم

---

### 2. **GET `/products/all` - تمام محصولات برای Dashboard فروشندگان** 
   - ✅ **تمام محصولات رو شامل می‌شود** (حتی بدون Offer)
   - ✅ فیلترهای سادهنشده
   - ✅ برای Panel فروشندگان
   - ✅ فروشندگان محصولات بدون offer می‌بینند

```bash
GET /products/all?search=گوشی&page=1&limit=20
```

**پاسخ**: تمام محصولات (با یا بدون offer)

---

## 📊 مقایسه دو Endpoint

| ویژگی | `/products` | `/products/all` |
|--------|-----------|-----------------|
| **منظر** | مشتریان | فروشندگان |
| **محصولات بدون Offer** | ❌ نمایش نمی‌شوند | ✅ نمایش می‌شوند |
| **فیلترهای قیمت** | ✅ فعال | ✅ فعال (اختیاری) |
| **فیلترهای موجودی** | ✅ فعال | ❌ ندارند |
| **فیلترهای تخفیف** | ✅ فعال | ❌ ندارند |
| **تعداد پارامتری** | 15+ | 6 |
| **استفاده** | سایت عمومی | Dashboard فروشنده |

---

## 🔧 پارامترهای Endpoint `/products/all`

### جستجو
```bash
GET /products/all?search=محصول
```

### صفحه‌بندی
```bash
GET /products/all?page=2&limit=15
```

### فیلتر دسته‌بندی
```bash
GET /products/all?categoryId=5
```

### ترتیب
```bash
GET /products/all?sortBy=newest      # جدیدترین
GET /products/all?sortBy=price_low   # ارزان‌ترین
GET /products/all?sortBy=price_high  # گران‌ترین
GET /products/all?sortBy=popular     # پرفروش‌ترین
```

### وضعیت محصول
```bash
GET /products/all?isActive=true      # فقط فعال
GET /products/all?isActive=false     # فقط غیرفعال
```

### ترکیب پارامترها
```bash
GET /products/all?categoryId=5&search=گوشی&sortBy=price_low&page=1&limit=10
```

---

## 💡 Use Cases

### برای مشتریان - مروری بر محصولات:
```typescript
// نمایش محصولات عمومی (فقط با قیمت)
fetch('/api/products?categoryId=5&sortBy=newest&limit=12')
  .then(res => res.json())
  .then(data => {
    // فقط محصولات با offer می‌آیند
    console.log(data.data);
  });
```

### برای فروشندگان - مدیریت محصولات:
```typescript
// دریافت تمام محصولات برای ایجاد offer
fetch('/api/products/all?search=&page=1&limit=20')
  .then(res => res.json())
  .then(data => {
    // تمام محصولات می‌آیند (شامل آنهایی بدون offer)
    data.data.forEach(product => {
      if (!product.offers || product.offers.length === 0) {
        console.log(`محصول ${product.name} هنوز offer ندارد - می‌توانید offer ایجاد کنید`);
      }
    });
  });
```

---

## 🎯 چرا دو Endpoint؟

1. **Performance**: محصولاتی که فروخته نمی‌شوند (بدون offer) را در صفحات عمومی نشان نمی‌دهیم
2. **UX**: مشتریان تنها محصولات موجود و قیمت‌گذاری شده را می‌بینند
3. **Business**: فروشندگان می‌توانند محصولاتی که هنوز قیمت ندارند را ببینند و برای آنها offer ایجاد کنند
4. **Filtering**: فروشندگان فقط به فیلترهای ضروری نیاز دارند

---

## 📝 Migration Guide

### اگر قبلاً از یک Endpoint استفاده می‌کردید:

**قبل (يك endpoint عمومی):**
```typescript
// محصولات معمولی
fetch('/api/products?page=1')

// محصولات براي فروشندگان
fetch('/api/products?page=1') // همان endpoint
```

**بعد (دو endpoint واضح):**
```typescript
// محصولات معمولی (مشتریان)
fetch('/api/products?page=1')

// محصولات برای فروشندگان
fetch('/api/products/all?page=1')
```

---

## 🚀 Quick Reference

### تمام محصولات (فروشندگان نیاز دارند):
```bash
GET /products/all?categoryId=5&search=&sortBy=newest&page=1&limit=20
```

### محصولات تخفیف‌دار برای مشتریان:
```bash
GET /products?categoryId=5&discountedOnly=true&sortBy=discount
```

### محصولات موجود برای مشتریان:
```bash
GET /products?categoryId=5&inStockOnly=true&sortBy=price_low
```

---

## ✅ خلاصه

- **اگر صفحه مشتری است** → استفاده کنید: `GET /products`
- **اگر صفحه فروشنده است** → استفاده کنید: `GET /products/all`
